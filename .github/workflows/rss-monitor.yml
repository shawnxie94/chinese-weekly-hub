name: RSS 停更巡检

on:
  schedule:
    - cron: '0 0 1 * *'  # 每月1日00:00运行
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  check-rss-updates:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          pip install feedparser requests beautifulsoup4 python-dateutil

      - name: Run RSS monitor script
        run: python scripts/rss_monitor.py

      - name: Create Pull Request
        if: env.RSS_UPDATED == 'true'
        uses: peter-evans/create-pull-request@v6
        with:
          token: ${{ secrets.ACCESS_TOKEN }}
          add-paths: README.md
          commit-message: "🤖 自动更新: RSS停更巡检"
          branch: rss-update
          delete-branch: true
          title: "🤖 RSS停更巡检更新"
          body: "自动生成的 RSS 停更巡检更新，请确认后合并。"
          base: main
